import styled from "styled-components"
import { palette } from "../../../../config/theme/theme"

const ContainerCompleteRegister = styled.div`
  background: ${palette.primaryColor};
  place-items: center;
  display: grid;
  height: 100vh;
  width: 100%;
`

export const ItemFormMaterial = styled.div``

export const ContainerFullHeightComponent = styled.div``
